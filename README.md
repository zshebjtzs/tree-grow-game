<p align="center">
  <img src="https://img.shields.io/badge/HTML5-E34F26?logo=html5&logoColor=white" alt="HTML5">
  <img src="https://img.shields.io/badge/JavaScript-ES6-F7DF1E?logo=javascript&logoColor=black" alt="JavaScript">
  <img src="https://img.shields.io/badge/Canvas-2D-000000" alt="Canvas 2D">
  <img src="https://img.shields.io/badge/Web%20Worker-AI-FF6B6B" alt="Web Worker">
  <img src="https://img.shields.io/badge/License-MIT-blue.svg" alt="License">
  <img src="https://img.shields.io/badge/PRs-welcome-brightgreen.svg" alt="PRs Welcome">
</p>

# 🌳 生长树：图论 (Growth Tree: Graph Theory)

> 基于图论拓扑与 Voronoi 地图的离线双人/人机策略战棋。

**语言**：[中文](./README.md)

---

## 📖 目录

- [项目简介](#项目简介)
- [功能特性](#功能特性)
- [技术栈](#技术栈)
- [项目结构](#项目结构)
- [快速开始](#快速开始)
  - [普通玩家（开箱即玩）](#普通玩家开箱即玩)
  - [开发者本地运行](#开发者本地运行)
- [核心规则手册](#核心规则手册)
- [AI 机制与难度](#ai-机制与难度)
- [贡献](#贡献)
- [许可证](#许可证)
- [致谢](#致谢)

---

## 项目简介

本项目是对 B站 UP主 **“浩辰-_-”** 原创纸上游戏《生长树》的数字化重制。在保留原版“图论与面积争夺”核心玩法的基础上，本项目进行了深度拓展：

- **自动化生成地形**：采用 Voronoi 图算法，每局自动生成 50 个多边形战棋区域。
- **大幅提升视觉体验**：引入 Lloyd 松弛算法，生成极度均匀的“蜂窝状”地图。
- **智能图论规则引擎**：实现严谨的图论回溯（直接摧毁、连锁摧毁、免疫堡垒、分支点冻结）。
- **内置离线 AI 引擎**：引入 Web Worker 构建非 LLM 的启发式 AI，提供 3 个不同难度的单机对战体验。

---

## 功能特性

| 模块 | 子功能 |
| --- | --- |
| **双人热座对战** | 本地同设备双人轮流操作，无需网络，无缝切换。 |
| **单机 AI 对战** | 基于启发式打分算法的 AI（非 LLM）。提供 **简单**（策略犯错）、**普通**（加权随机）、**困难**（绝对理智择优）3 档难度。 |
| **地图生成引擎** | 基于 `d3-delaunay`，每次开局生成完全不同的 50 区地图。配合 Lloyd 松弛算法及极长/极短边阻断，杜绝视觉伪邻接（飞线）。 |
| **图论战斗核心** | 生长受限于“单步穿线”；战斗时支持**直接摧毁分支点**、**连锁摧毁向上回溯**、**免疫堡垒**（>3条连接）绝对防御。 |
| **战略冻结机制** | 当敌方切断你的某条树枝时，幸存的上游分支点将在**下一回合被冻结**，无法生长，有效防止双方陷入局部“绞肉机”拉锯战。 |
| **本地单文件存档** | 基于浏览器 `File System Access API`，支持自由保存和读取 `*.sav` 文件到本地电脑。 |
| **纯前端免环境** | 所有代码打包为一个 `bundle.js`，配合 `index.html` 和 `style.css`，**无需安装 Node.js 或数据库**，双击即可运行。 |

---

## 技术栈

### 前端与渲染

| 分类         | 技术                                             |
| ------------ | ------------------------------------------------ |
| 核心框架     | 原生 HTML5 + CSS3 + JavaScript (ES6 Modules)     |
| 渲染引擎     | HTML5 Canvas 2D API（无三方 UI 框架）            |
| 打包工具     | `esbuild`（极速打包，生成单文件 bundle）         |

### 算法与计算

| 分类         | 技术                                             |
| ------------ | ------------------------------------------------ |
| 地形生成     | `d3-delaunay`（Voronoi 图生成）                  |
| 地图平滑     | Lloyd 松弛算法（Lloyd's algorithm）              |
| 图论计算     | BFS 广度优先搜索（用于寻路、距离判断、回溯连坐） |
| AI 引擎      | Web Worker 独立线程 + 启发式打分（Heuristic）    |

---

## 项目结构

```
tree-grow-game/
├── index.html                 # 应用入口 HTML，定义 Canvas 挂载点
├── css/
│   └── style.css              # 全局样式（UI 面板、按钮、布局）
├── js/
│   ├── bundle.js              # esbuild 打包后的最终单文件（普通用户双击运行）
│   ├── main.js                # 主入口，负责 UI 控制、回合调度、AI 触发
│   ├── core/                  # 底层核心模块
│   │   ├── MapManager.js      # Voronoi 地图生成、Lloyd 松弛、飞线阻断、邻接表维护
│   │   └── EventManager.js    # 鼠标点击碰撞检测与区域识别
│   ├── game/                  # 游戏数据结构与业务规则
│   │   ├── Node.js            # 有向树节点数据结构（坐标、归属、父子关系、冻结状态）
│   │   ├── GameState.js       # 全局面板状态管理（节点数组、回合控制、切回及解冻）
│   │   └── RuleEngine.js      # 图论核心规则（BFS 寻路、跨区距离、堡垒判断、连坐摧毁）
│   ├── renderer/              # 渲染层
│   │   └── CanvasDrawer.js    # Canvas 地图渲染器（区域编号、红蓝节点、连线、死亡动画）
│   └── ai/                    # AI 模块（Web Worker 独立线程）
│       ├── AIWorker.js        # 后台线程接收数据并处理 AI 计算
│       └── AIController.js    # 启发式打分引擎，生成最佳生长动作
├── LICENSE                    # MIT 开源许可证
└── README.md                  # 项目说明文档
```

---

## 快速开始

### 普通玩家（开箱即玩）

本项目**无需安装任何开发环境**（无需 Node.js，无需数据库）。
1. 前往本项目的 **[Releases](https://github.com/zshebjtzs/tree-grow-game/releases)** 页面。
2. 下载最新版本的 `tree-grow-game.zip`。
3. 解压后，找到 `index.html`，**直接双击** 用浏览器（Chrome/Edge 皆可）打开即可游玩。

### 开发者本地运行

如果你想在 VS Code 中修改代码或进行二次开发：

1.  **克隆仓库**

   ```bash
   git clone https://github.com/zshebjtzs/tree-grow-game.git
   cd tree-grow-game
   ```

2.  **运行开发模式**

   本项目采用原生 ES Modules 开发，打开 VS Code 后，安装 **[Live Server](https://marketplace.visualstudio.com/items?itemName=ritwickdey.LiveServer)** 插件。在 `index.html` 上右键点击 **"Open with Live Server"**，即可在浏览器中实时调试。

3.  **重新打包（生成 bundle.js）**
   当你修改了 `js/main.js` 及各个模块文件后，为了让你或者朋友可以 `双击 index.html` 直接运行，需要重新打包：
   ```bash
   npm install -g esbuild # 如果没装过 esbuild
   npx esbuild js/main.js --bundle --outfile=js/bundle.js --external:d3-delaunay --format=esm
   ```


---

## 核心规则手册

《生长树：图论》的核心玩法建立在地理区域控制与图论节点网络之上。以下为完整的游戏规则说明。

### 一、 基础术语定义
*   **根节点**：游戏开局时，双方基地内各自动生成 3 个起始节点。根节点是阵营的最底层据点，一旦全部被摧毁则立即判定为失败。
*   **叶节点**：玩家每回合行动时，新生长出的末端节点。
*   **分支点**：连接数量 ≥3 条树枝的节点。它承担着连接整棵树木上下级结构的关键作用。

### 二、 回合生长与区域扩张
玩家在每一个回合中，必须选择一个己方节点作为起点，伸出一根新树枝，并在终点落下一个新的叶节点。

*   **单步跨越**：绝大多数的生长行为，只能穿过 **1 条区域边界线（黑线）**，进入一个与当前区域紧邻的新区域。禁止直接跨越多个区域。
*   **初生特权**：由**根节点**伸出的**第一根枝条**，允许跨越最多 **2 块** 己方已占据的区域。
*   **领地限制**：枝条在延伸的过程中，**绝对不可跨越**任何已被占据的区域（无论是己方还是敌方的领地，均不可跨越）。
*   **据点独占**：除己方基地外，地图上的任意一个普通区域**最多只能容纳 1 个己方节点**。
*   **禁止回溯**：玩家不可向己方基地所在的区域生长。

### 三、 图论战斗规则
当己方的新生叶节点，落入敌方已有节点所在的**同一区域**时，触发战斗。战斗结果根据敌方节点的连接结构分为以下情况：

*   **直接摧毁（斩首行动）**：如果该敌方节点为连接数 **≤3 条** 的分支点（即脆弱节点），该节点将被**连根拔起**。系统会顺藤摸瓜，将其下辖的所有子节点及子树一并从地图上抹除。
*   **连锁摧毁（枝干修剪）**：如果该敌方节点是叶节点或单链路节点，系统将沿着树的结构向上回溯，寻找离该节点**最近的分支点**。该分支点自身幸存，但分支点与受击节点之间的整段枝干将被彻底切断并摧毁。
*   **免疫堡垒（绝对防御）**：如果该敌方节点为连接数 **>3 条** 的超级节点，它将被视为一座无法逾越的堡垒。该节点不仅无法被摧毁，且敌方的树枝**无法踏入**该堡垒所在的区域。

### 四、 节点冻结机制
为了限制双方在局部区域内的反复拉扯，游戏引入了防御性冻结规则。

*   **触发条件**：当敌方触发了上述的**连锁摧毁**（枝干修剪），且被剪断的起始分支点在战斗中幸存。
*   **冻结效果**：该幸存的起始分支点在**玩家（防守方）的下一个回合内**，将无法被选中作为生长的起点。
*   **状态解除**：防守方只要在该回合内完成**任意一次合法行动（成功生长一步或直接点击跳过回合）**，冻结状态会立即自动清除。

### 五、 胜利条件
达成以下任意一项条件，游戏立即结束并宣告胜者：
1.  **战略突入**：玩家的树枝成功踏入**敌方基地**所在的区域。
2.  **全面歼灭**：成功摧毁敌方阵营的**所有 3 个根节点**。

---

## AI 机制与难度

- AI 运行在独立的 **Web Worker** 线程中，不会因为算路而卡死主界面。
- AI 采用**启发式打分**策略，即时评估“能否吃子”、“能否突入敌方基地”、“风险规避”等因素。
- **简单难度**：策略具有随机性，适合新手熟悉规则。
- **普通难度**：加权随机选择高分动作，有一定战术意识，但不死板。
- **困难难度**：绝对理智，永远只选择当前打分最高的最优解，适合极限锻炼。

---

## 贡献

欢迎任何形式的贡献！请遵循以下流程：

1. Fork 本仓库
2. 创建你的功能分支：`git checkout -b feature/amazing-feature`
3. 提交你的更改：`git commit -m '添加某个精彩功能'`
4. 推送到分支：`git push origin feature/amazing-feature`
5. 发起 Pull Request

如果你有好的想法，也可以提交issue！

---

## 许可证

本项目采用 MIT 许可证。详见 [LICENSE](./LICENSE) 文件。

© 2026 生长树：图论 (zshebjtzs)

---

## 致谢

- 特别感谢 B站 UP主 **浩辰-_-** 原创的纸上桌游《生长树》提供的核心玩法灵感。
- 感谢开源社区提供的杰出工具：`d3-delaunay`, `esbuild`。
- 感谢每一位参与测试并提供反馈的玩家朋友，是你们的建议让这个游戏变得更加严谨和有趣。
